using Microsoft.AspNetCore.SignalR;

namespace mvcproj.Hubs
{
    public class CommentsHub:Hub
    {
        //public async Task SendComment(string userName, string email, string commentText)
        //{
        //    await Clients.All.SendAsync("ReceiveComment", userName, email, commentText);
        //}
    }
}
